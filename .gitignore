# Julia / Pkg
Manifest.toml
docs/Manifest.toml
docs/build/
docs/site/

# Local runs
data/
results/
output/
.distsshkitqueue/

# Tests & CI artifacts
.ci-cache/
*.jl.cov
*.jl.*.cov
*.jl.mem
coverage/
lcov.info

# OS / editor noise
.DS_Store
Thumbs.db
*~
.vscode/settings.json

# Secrets / local tooling
.env
.env.*
!.env.example
*.pem
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub
.cursor/

# Local dev notes (not shared)
ROADMAP.ja.local.md

# Staged DistSSHKit demos (copied at E2E start; rsync still sends them)
testenv/example-job/demos/
# Kit go collect. Not in example-job/.gitignore: setup rsync honors that file
# and would drop slot artifacts (`go_out/` was ignored, so pi_file never landed).
testenv/example-job/e2e_kit_out/
