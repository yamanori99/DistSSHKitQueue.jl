# Kit run outputs / logs. Do not ignore `demos/` — setup rsync honors this file,
# and E2E copies DistSSHKit demos there so workers must receive them.
go_out/
output/
.distsshkit/
*.log
Manifest.toml
